const express = require('express');
const router =express.Router();
const socketEvents = require('../controllers/socket_event');

router.get('/get_hosts', (req,res,next)=>{
    
});

module.exports = router;
